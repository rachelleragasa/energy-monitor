# Base URL of the Python backend the frontend talks to.
# Local dev (default if unset): http://localhost:8000
# On Vercel, set this to your deployed backend's public URL.
BACKEND_URL=http://localhost:8000
